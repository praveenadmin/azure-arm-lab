name: Deploy Storage

on:
  workflow_dispatch:

jobs:
  deploy-storage:
    uses: azure-arm-lab/BH-Reusable-Templates/.github/workflows/reusable-deploy.yaml@main
    with:
      templateFile: templates/storage.jsonc
      parametersFile: parameters/storage/dev.jsonc
      resourceGroup: arm-lab-rg
    secrets:
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
